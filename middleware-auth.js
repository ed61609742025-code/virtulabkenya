// ============================================================
//  VirtuLab Kenya — JWT Authentication Middleware
//  Phase 1, Week 3: To be built
// ============================================================

// This middleware will:
// 1. Read the Authorization header from every request
// 2. Verify the JWT token
// 3. Attach the user object to req.user
// 4. Reject requests with invalid or missing tokens

module.exports = (req, res, next) => {
  next(); // placeholder — authentication added in Week 3
};
